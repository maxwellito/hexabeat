/**
 * Here is the base config
 * Where all the default configuration
 * is defined.
 */

export default {
  livesets: [
    '/hexabeat-livesets/tr808.json',
    '/hexabeat-livesets/chiptune.json'
  ],
  repositories: ['samaaron/sonic-pi', 'maxwellito/matrx']
};
